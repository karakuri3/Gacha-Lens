export const dynamic = "force-dynamic";
export const revalidate = 0;

export default function ColdShellDiagnosticPage() {
  return (
    <main>
      <h1>cold-shell-diagnostic</h1>
      <p>root-layout-only SSR probe</p>
    </main>
  );
}
